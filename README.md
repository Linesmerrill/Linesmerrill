# Generated metrics

This branch holds SVG cards rendered by [`.github/workflows/metrics.yml`](../../blob/main/.github/workflows/metrics.yml)
on `main`. It is written by CI — don't edit it by hand.

Kept off `main` so bot commits don't churn the main history, and so the
workflow doesn't need to bypass branch protection.
